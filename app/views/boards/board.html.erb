ボードの詳細

<%= form_for @new_filed, :url => posts_create_path do |new_filed| %>
  <%= new_filed.text_field :comment %>
  <%= new_filed.hidden_field :board_id %>
  <%= new_filed.submit "投稿！！" %>
<% end %>

<hr>
<% @filed.each do |d| %>
 <p>
    <%= d.comment %>
 </p>
 <% end %>
 <hr>


<%= button_to "homeに戻る", {controller: 'boards', action:'index'}, {method: :get} %>
